/**
 * Room Controller Placeholders for Phase 1
 */

export const createRoom = async (req, res) => {
  return res.status(501).json({
    success: false,
    message: 'Room creation endpoint placeholder',
  });
};

export const getRoomDetails = async (req, res) => {
  return res.status(501).json({
    success: false,
    message: 'Get room details endpoint placeholder',
  });
};

export const joinRoom = async (req, res) => {
  return res.status(501).json({
    success: false,
    message: 'Join room endpoint placeholder',
  });
};
